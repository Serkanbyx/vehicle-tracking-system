export enum GeofenceShape {
  POLYGON = "polygon",
  CIRCLE = "circle",
}

export enum GeofenceDirection {
  ENTER = "enter",
  EXIT = "exit",
  BOTH = "both",
}

export enum GeofenceAppliesTo {
  ALL = "all",
  SPECIFIC = "specific",
}
